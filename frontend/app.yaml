runtime: nodejs20
env: standard
instance_class: F1

handlers:
  # Mengarahkan semua request ke index.html (cocok untuk SPA seperti React/Vue)
  - url: /.*
    static_files: build/index.html
    upload: build/index.html
  - url: /
    static_dir: build